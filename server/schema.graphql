type Query {
  # query {
  #   job(id: "rJKAbDd_z") {
  #	    id, title, description, company {
  #       id, name
  #     }
  #   }
  # }
  job(id: ID!): Job
  # query {
  #   jobs {
  #     id, title, description, company {
  #       id, name
  #     }
  #   }
  # }
  jobs: [Job]
  company(id: ID!): Company
  companies: [Company]
}

type Company {
  id: ID!
  name: String
  description: String
  jobs: [Job]
}

type Job {
  id: ID!
  title: String
  company: Company
  description: String
}
